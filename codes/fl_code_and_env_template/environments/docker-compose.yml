version: '2'
services:
    base:
        build:
            context: .
            dockerfile: base/Dockerfile
        image:  ljiang98/base
    pytorch-mpi:
        build:
            context: .
            dockerfile: pytorch-mpi/Dockerfile
        image:  ljiang98/pytorch-mpi
        depends_on:
          - base
